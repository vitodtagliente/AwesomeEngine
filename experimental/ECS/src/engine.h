#pragma once

#include <map>
#include <vector>
#include "system.h"

namespace ECS
{
	class Engine
	{
	public:

		Engine() = default;

		void update(const float t_deltaTime)
		{

		}

	private:

		// std::map<size_t, > m_systems;
	};
}